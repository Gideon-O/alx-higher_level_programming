#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b()):
    sum_tuple = ()
    for i in range(0, 2):
        sum_tuple.append(tuple_a[i] + tuple_b[i])


if __name__ == "__name__":
  add_tuple(tuple_a=(), tuple_b())
